import BitcoinButton from "../components/BitcoinButton"

function Home(){
    return(
    <div className="min-h-screen bg-[color:var(--brand-soft)]/20 flex items-center justify-center">
      <BitcoinButton />
    </div>
    )
}

export default Home